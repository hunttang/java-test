allprojects {
    repositories {
        mavenCentral()
    }

    apply plugin: 'idea'
}

subprojects {
    group 'com.sensetime.test'
    version '1.0'
}

task wrapper(type: Wrapper) {
    gradleVersion = '2.10'
}
